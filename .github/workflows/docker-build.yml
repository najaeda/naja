name: docker-build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/naja_edit

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - distro: alpine
            dockerfile: docker/Dockerfile.alpine
            tag: latest
            build_args: ""
            naja_edit_path: /naja/naja_edit
          - distro: rockylinux8
            dockerfile: docker/Dockerfile.rockylinux
            tag: rockylinux-8
            build_args: |
              ROCKY_VERSION=8
            naja_edit_path: /naja/bin/naja_edit
          - distro: rockylinux9
            dockerfile: docker/Dockerfile.rockylinux
            tag: rockylinux-9
            build_args: |
              ROCKY_VERSION=9
            naja_edit_path: /naja/bin/naja_edit
    steps:
      -
        name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: true
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v4
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      -
        name: Install regress dependencies
        run: sudo apt-get update && sudo apt-get install -yq make rsync
      -
        name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        if: github.repository == 'najaeda/naja' && github.event_name == 'push' && github.ref == 'refs/heads/main'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      -
        name: Build (load for test)
        uses: docker/build-push-action@v7
        with:
          load: true
          file: ${{ matrix.dockerfile }}
          tags: ${{ env.IMAGE_NAME }}:${{ matrix.tag }}
          build-args: ${{ matrix.build_args }}
      -
        name: Run naja_edit smoke test
        run: |
          docker run --rm ${{ env.IMAGE_NAME }}:${{ matrix.tag }} --help
          docker run --rm --entrypoint /bin/sh ${{ env.IMAGE_NAME }}:${{ matrix.tag }} -c 'test "$(id -u)" != "0" && test -w /work'
          docker run --rm --user 12345:0 --entrypoint /bin/sh ${{ env.IMAGE_NAME }}:${{ matrix.tag }} -c 'test "$(id -u)" = "12345" && test -w /work && ${{ matrix.naja_edit_path }} --help >/dev/null 2>&1'
      -
        name: Verify portable Rocky bundle
        if: startsWith(matrix.distro, 'rockylinux')
        run: |
          docker run --rm --entrypoint /bin/sh ${{ env.IMAGE_NAME }}:${{ matrix.tag }} -c '
            set -eu
            test -x /naja/bin/naja_edit
            test ! -e /naja/naja_edit
            require_lib() {
              found=0
              for lib in "$@"; do
                if [ -e "${lib}" ]; then
                  found=1
                fi
              done
              if [ "${found}" = 0 ]; then
                echo "Missing bundled library matching: $*"
                ls -la /naja/lib
                exit 1
              fi
            }
            require_lib /naja/lib/libcapnp*.so*
            require_lib /naja/lib/libkj*.so*
            require_lib /naja/lib/libpython3*.so*
            require_lib /naja/lib/libtbb*.so*
            require_lib /naja/lib/libstdc++.so*
            LD_LIBRARY_PATH= /naja/bin/naja_edit --help >/dev/null 2>&1
            : > /tmp/naja_bundle.ldd
            for elf in /naja/bin/naja_edit /naja/lib/*.so* /naja/lib/python/*/*.so*; do
              if [ -e "${elf}" ] && ldd "${elf}" >/tmp/naja_one.ldd 2>/dev/null; then
                {
                  echo "## ${elf}"
                  cat /tmp/naja_one.ldd
                } >> /tmp/naja_bundle.ldd
              fi
            done
            cat /tmp/naja_bundle.ldd
            ! grep -q "not found" /tmp/naja_bundle.ldd
            grep -E "libcapnp.* => /naja/lib/" /tmp/naja_bundle.ldd
            grep -E "libkj.* => /naja/lib/" /tmp/naja_bundle.ldd
            grep -E "libpython3.* => /naja/lib/" /tmp/naja_bundle.ldd
            grep -E "libtbb.* => /naja/lib/" /tmp/naja_bundle.ldd
          '
      -
        name: Run megaboom regress
        run: |
          make -C ${{ github.workspace }}/regress/designs/verilog/megaboom clean
          make -C ${{ github.workspace }}/regress/designs/verilog/megaboom \
            NAJA_EDIT="docker run --rm --user $(id -u):$(id -g) -v ${{ github.workspace }}:${{ github.workspace }} -w ${{ github.workspace }}/regress/designs/verilog/megaboom --entrypoint ${{ matrix.naja_edit_path }} ${{ env.IMAGE_NAME }}:${{ matrix.tag }}"
      -
        name: Build and push
        uses: docker/build-push-action@v7
        if: github.repository == 'najaeda/naja' && github.event_name == 'push' && github.ref == 'refs/heads/main'
        with:
          push: true
          file: ${{ matrix.dockerfile }}
          tags: ${{ env.IMAGE_NAME }}:${{ matrix.tag }}
          build-args: ${{ matrix.build_args }}
