# -*- mode: CMAKE explicit-buffer-name: "CMakeLists.txt<lefdef/src/def>" -*-

 add_subdirectory(def)
 add_subdirectory(defzlib)
 add_subdirectory(defdiff)
 add_subdirectory(defrw)
 add_subdirectory(defwrite)
